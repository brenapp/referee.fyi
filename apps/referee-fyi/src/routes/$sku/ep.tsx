import { createFileRoute } from '@tanstack/react-router'

export const EventPartnerSummary: React.FC = () => {
    
}


export const Route = createFileRoute("/$sku/ep")({
  component: EventPartnerSummary,
});
